# Stream V3 车辆清洗检测系统依赖
# 安装命令: pip install -r requirements.txt

# ========== Web 框架 ==========
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
websockets>=12.0

# ========== 深度学习/AI ==========
# YOLOv8 车辆检测
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.200

# ========== OCR 车牌识别 (二选一或都装) ==========
# 方案1: PaddleOCR (推荐，中文识别效果好)
paddleocr>=2.7.0
paddlepaddle>=2.5.0

# 方案2: EasyOCR (备选)
# easyocr>=1.7.0

# ========== 图像处理 ==========
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# ========== 数据处理 ==========
pyyaml>=6.0.1
python-dateutil>=2.8.2

# ========== 数据库 ==========
aiosqlite>=0.19.0

# ========== 工具库 ==========
tqdm>=4.66.0
requests>=2.31.0
aiohttp>=3.9.0

# ========== 可选: GPU 加速 ==========
# 如果有 NVIDIA GPU，请安装 CUDA 版本的 PyTorch:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
